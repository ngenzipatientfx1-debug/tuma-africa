# ===========================================
# Tuma-Africa Link Cargo - Environment Variables
# ===========================================
#
# QUICK START:
# 1. Copy this file: cp .env.example .env
# 2. Replace placeholder values below
# 3. Generate secrets: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# ===========================================

# ===========================================
# DATABASE (REQUIRED)
# ===========================================
DATABASE_URL=postgresql://username:password@localhost:5432/tuma_africa
# Example: postgresql://tuma_user:SecurePass123@localhost:5432/tuma_africa

# ===========================================
# AUTHENTICATION SECRETS (REQUIRED)
# ===========================================
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=change-this-to-a-random-secret-string-min-32-chars
JWT_SECRET=change-this-to-another-random-secret-string-min-32-chars
JWT_EXPIRES_IN=7d

# ===========================================
# APPLICATION SETTINGS
# ===========================================
NODE_ENV=production
PORT=5000

# ===========================================
# OPTIONAL SETTINGS
# ===========================================
# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# File Upload Settings
MAX_FILE_SIZE=2097152
UPLOAD_DIR=./uploads

# Email Configuration (future feature)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# ===========================================
# REPLIT AUTH (only needed on Replit)
# ===========================================
# REPL_ID=your-repl-id
# REPLIT_DOMAINS=your-repl.replit.dev
# ISSUER_URL=https://replit.com/oidc
